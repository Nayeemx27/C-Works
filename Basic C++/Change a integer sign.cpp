/*Write a program which will take an integer number as input
the number is positive, then make it negative and if the
number is negative, then make it positive. */

#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin>>n;

    n=n*(-1);
    cout<<n;

    cout<<endl;
    return 0;
}
